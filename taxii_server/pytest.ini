[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
env =
    # Define environment variables for tests if needed, e.g., for a test database URL
    # SQLALCHEMY_DATABASE_URL=postgresql://testuser:testpass@localhost/testdb
    # Or use an in-memory SQLite for some tests:
    # SQLALCHEMY_DATABASE_URL=sqlite:///./test.db
    # Ensure settings load this appropriately, or override in test setup
